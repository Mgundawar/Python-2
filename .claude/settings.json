{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash(git *)",
        "if": "Bash(git push*)",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'Hook fired at' > hook-test.txt && date >> hook-test.txt && gh pr create --fill --draft >> hook-test.txt 2>&1 || echo 'gh pr create failed' >> hook-test.txt"
          }
        ]
      }
    ]
  }
}